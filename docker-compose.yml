services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: medcare
      POSTGRES_USER: medcare
      POSTGRES_PASSWORD: medcare
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U medcare -d medcare"]
      interval: 5s
      timeout: 5s
      retries: 20

  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 5000
      SESSION_SECRET: change-me-in-production
      DATABASE_URL: postgresql://medcare:medcare@db:5432/medcare
    ports:
      - "5000:5000"

volumes:
  pgdata:
